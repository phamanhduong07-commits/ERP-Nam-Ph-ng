{
  "enabledPlugins": {
    "agent-sdk-dev@claude-plugins-official": true,
    "claude-code-setup@claude-plugins-official": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.env.CLAUDE_TOOL_INPUT_FILE_PATH||''; if(p.includes('frontend') && (p.endsWith('.tsx')||p.endsWith('.ts'))) { const {execSync}=require('child_process'); try { execSync('cd \\\"d:\\\\NAM_PHUONG_SOFTWARE\\\\DU LIEU MPS\\\\erp-nam-phuong\\\\frontend\\\" && npx tsc --noEmit 2>&1', {stdio:'inherit'}); } catch(e) { process.exit(1); } }\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const p=process.env.CLAUDE_TOOL_INPUT_FILE_PATH||''; const blocked=['.env','/alembic/versions/']; const hit=blocked.find(b=>p.includes(b)); if(hit){ console.error('BLOCKED: File nhay cam ['+hit+'] - xac nhan thu cong truoc khi sua.'); process.exit(2); }\""
          }
        ]
      }
    ]
  }
}
